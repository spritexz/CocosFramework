
/** 核心组件ID */
export enum CoreComponentIds {
    Movable,
    Position,
    Destroy,
    GameBoardCache,
    GameBoard,
    GameBoardElement,
    Input,
    Interactive,
    Resource,
    View,
    Score,
    /** 总组件数(放到枚举最后, 用于计数) */
    TotalComponents,
}